apiVersion: v1
kind: Service
metadata:
  name: nginx
  namespace: nginx-ingress
spec:
  ports:
    - port: 80
      name: http
    - port: 443
      name: https
  selector:
    app: nginx
  externalIPs:
    - 192.169.7.138
